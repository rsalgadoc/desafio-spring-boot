version: "3.8"
services:
  mainapp:
    image: rsalgadoc/taskmanagement:latest
    container_name: Bankapp
    ports:
      - "8081:8080"
    networks:
      - bankapp
    restart: always
networks:
  bankapp: